extends layout

block body
  .container
    .row
      .col-sm-6
        h1 Movie recommendation demo
        p= `Total number of users registered: ${totalUsers}`
        p= `Total number of reviews: ${totalReviews}`
